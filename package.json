{
  "name": "informatix",
  "version": "1.0.0",
  "private": true,
  "description": "Informatix - Educational platform for algorithms and programming",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "serve": "^14.2.0"
  }
}
